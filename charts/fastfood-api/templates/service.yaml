apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastfood-api.fullname" . }}
  labels:
    {{- include "fastfood-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastfood-api.selectorLabels" . | nindent 4 }}
